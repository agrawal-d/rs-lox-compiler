// Recursive Fibonacci function (slow)
function fib(n) {
    if (n<2) {
        return n;
    }

    return fib(n-1) + fib(n-2);
}

var start = Clock();
print("fib(10) = " + fib(30));
var dur = Clock() - start;
print("Time: " + dur + "ms");