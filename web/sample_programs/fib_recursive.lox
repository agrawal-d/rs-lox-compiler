// Recursive Fibonacci function (slow)
function fib(n) {
    if (n<2) {
        return n;
    }

    return fib(n-1) + fib(n-2);
}

var start = clock();
print("fib(30) = " + fib(30));
var dur = clock() - start;
print("Time: " + dur + "ms");